import { createClient } from '@supabase/supabase-js'; import { sendFonnteWA } from './src/lib/fonnte'; import 'dotenv/config'; const supabase = createClient(process.env.SUPABASE_URL!, process.env.SUPABASE_SERVICE_KEY!); supabase.from('rt').select('nomor_rt, nama_ketua, no_wa_rt').not('no_wa_rt', 'is', null).limit(1).then(async ({ data, error }) => { if (error) { console.error('Error fetching RT:', error); return; } if (!data || data.length === 0) { console.log('No RT with WhatsApp number found'); return; } const rt = data[0]; console.log(`Testing notification to RT ${rt.nomor_rt} (${rt.nama_ketua})`); console.log(`WhatsApp number: ${rt.no_wa_rt}`); const testMessage = '[TEST] Ini adalah pesan tes dari sistem Si-Manggis untuk memastikan notifikasi ke RT berfungsi.'; console.log('Sending test message...'); const result = await sendFonnteWA({ target: rt.no_wa_rt, message: testMessage }); console.log('Result:', result); if (result.success) { console.log('✅ Test message sent successfully!'); } else { console.log('❌ Failed to send test message:', result.error); } });
